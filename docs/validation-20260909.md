# 整合驗收紀錄 — 2026-09-09

## 自動檢查

- Python 常規測試：53 通過，1 個真實 SAM2 測試預設不執行；該 SAM2 測試另行開啟並通過。
- JavaScript：6 項通過，涵蓋連續修改期間的保存序列、衝突保留、精確 RLE 孔洞、遮罩移動、旋轉框及歷史紀錄。
- Python 套件相依檢查通過，前端 JavaScript 語法檢查通過。

## 三節點連續操作

在新的獨立環境、暫存工作資料與真實 Qt Chromium 頁面執行：

建立專案 → 匯入兩批共 4 張圖片與標註 → 修改類別 → 自動／手動儲存 → 切圖續編 → 複製 → 復原／重做 → 實際 GrabCut 候選生成 → 接受遮罩 → Qt 滑鼠事件繪製矩形 → 核准 → 驗證 → 原生資料集與 ZIP 匯出 → 重新載入頁面。

資料、修訂與核准狀態持續保留。無前端頁面錯誤。1280×720、1440×900、1920×1080 的畫布與主頁無水平溢出；右側物件清單優先顯示、AI 面板可收合。

另以實際原生 MainWindow 測試啟動及非同步存檔後關閉，確認關閉等待期間不接受新編輯。

## 既有真實資料

使用既有的 52 張核准影像／52 個物件、4 個拍攝批次，驗證 7 種格式：Native、COCO、YOLO Detection、YOLO Segmentation、LabelMe、Classification、JSONL。

- 七種格式均完成驗證、匯出及再次匯入。
- 原始影像與輸出影像 SHA-256 一致，來源檔案未被修改。
- Native／COCO／Classification／JSONL 的 52 個精確 mask 含孔洞逐像素相同，穩定物件 ID 保留。
- YOLO 與 LabelMe 的格式損失先告知、需明確確認，並記錄在 conversion_report。
- 每格式約 14.2–17.8 秒，合計約 117.8 秒，為這批資料在目前設備的量測。

詳細原始報告位於 `verification/pipeline-real-20260909.json`，不納入原始碼版控。

## GPU 與相機

- 新 `.venv`、新本機模型目錄，SAM2 完全離線執行。
- RTX 4050 Laptop GPU、CUDA float16：合成有孔工件框提示與正／負點提示均成功，提示違反數 0，孔洞保留。
- 最近一次模型冷載入約 5.20 秒；快取後點提示推論約 67.7 ms。第一次新環境初始化曾達約 80 秒，故介面載入階段使用不確定進度。
- 真實 USB2.0 HD UVC WebCam：640×480 擷取、Canny 即時預覽、31 影格 MP4 錄影完成並驗證，錄影時間約 2.07 秒；停止後狀態為 stopped。
- 硬體測試的影像只寫入測試暫存資料夾，完成後清除；保留 `verification/hardware-camera-20260909.json` 的設備與結果資料。

## 已界定的範圍

- 未聲稱完成長時間相機壓力測試、所有品牌工業相機或全新 Windows 安裝機驗收。
- 模型訓練、產線部署、第三方標註平台完整重製不在本次影像資料流程範圍。
- 帶 EXIF 旋轉標記的 JPEG 以原像素方向在工作台操作；原生格式／JSONL 可無損交換，會自行旋轉圖片的其他輸出格式在正規化方向前明確阻擋。
- 即時處理有獨立預覽，錄影保存原始影像，沒有宣稱兩路延遲對齊的演算法錄影。

驗收報告描述實際完成的檢查，不將測試通過解讀為所有硬體與任意資料均不存在問題。
